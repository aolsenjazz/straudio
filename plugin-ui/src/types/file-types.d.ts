// For whatever reason, the compiler doesn't like it when other type
// declarations exist in the same file as this one. Maybe the import call?
// Either way, file type declarations here.
declare module '*.png';
